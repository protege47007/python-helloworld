users = ["Ope", "Josh", "Ibk"]

other_users = users.copy #creates a copy of users list

#bool int, float, strings, tuple are immutable

#lists, sets, dict -> keep referene to the original and mutate them on change 